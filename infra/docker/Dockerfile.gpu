# syntax=docker/dockerfile:1.6

FROM nvidia/cuda:12.2.0-cudnn9-devel-ubuntu22.04

ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 \
    python3-pip \
    python3-venv \
    python3-dev \
    build-essential \
    cmake \
    git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /opt/swarmopt

COPY infra/requirements.txt infra/requirements.txt
COPY infra/requirements-dev.txt infra/requirements-dev.txt

RUN python3 -m pip install --upgrade pip && \
    python3 -m pip install --no-cache-dir -r infra/requirements.txt -r infra/requirements-dev.txt

COPY . .

RUN python3 -m pip install --no-cache-dir -e .

CMD ["python3", "tests/examples/huntington_speech_tda.py"]


